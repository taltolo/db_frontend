import NetworkInformation from '../NetworkInformation/NetworkInformation';
const EditCard = ({ network }) => {
  return <>{/* <NetworkInformation network={network} /> */}</>;
};

export default EditCard;
